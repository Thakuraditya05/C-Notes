#include<iostream>// input output stream 
using namespace std;
int main(){ 
//odd
            // int n;
            // cin>>n;
            // if(n%2==0)cout<<"even";
            // else cout<<"odd";

//5 divisibility 
            // int n;
            // cin>>n;
            // if(n>=0)cout<<n;
            // else cout<<(-n);
//profit and loss
            // int sp,cp;
            // cin>>sp>>cp;
            // if(sp>cp)cout<<"ptofit"
            //else loss

//three digit number 
            // int num;
            // cin>>num;
            // if(num>=100&&num<1000)cout<<"yes it is";
            // else cout<<"no";
//num divisible by 5 and 3
            // int num=40;
            // if((num%3==0))
            //      if( num%5==0)cout<<"yes it is";
            //      else cout<<"no";
            // else cout<<"nope";
// divisibility by 5 0r 3
        // int num=40;
        // if((num%3==0))
        //  if( num%5==0)cout<<"yes it is";    
        //  else cout<<"by 3 but not 5";
        // else cout<<"nope by 3 AND 5";
//  greatest of two number 
        // int x,y,z;
        // cin>>x>>y>>z;
        // if(x>y){
        //         if(x>z)cout<<"x is greatest"<<x<<endl; 
        //         else cout<<"z is greatest"<<z<<endl; 

        // }else{ 
        //         if(y>z)cout<<"y is greatest"<<y<<endl;//y>x>z
        //         else cout<<"z is greatest"<<z<<endl;//y>x
        // }
// check for char type 
        // char ch;
        // cin>>ch;
        // int y=int(ch);
        // if((y>=97&&y<=122)||(y>=65&&y<90)){
        //         cout<<"it is char type"<<" "<<ch;
        // }
// divisible by 3 5 but not 15
        // int x;
        // cin>>x;
        // if(x%3==0||x%5==0){
        //     if(x%15==0)cout<<"divisible by all three numbers"<<x<<" ";
        //     else cout<<"divisible by 3 and 5 numbers"<<x<<" ";
        // }else 
        //     cout<<" non divisible any  numbers"<<x<<" ";
//print the marks numbers 
//wap to check given number is vowel or not 
// tell  the about three sides 
// leap year 
        // int x;
        // cin>>x;
        // if ((x % 4 == 0 && x % 100 != 0) || (x % 400 == 0)) {
        // cout << "x is a leap year";
        // } else {
        // cout << "x is not a leap year";
        // } 
// parimete is greater than area 
        // int l, b;
        // cin>>l>>b;
        // if((2*(l+b))>(l*b)){
        //         cout<<"perimeter is greater than area";
        // }else cout<<"area is greater than perimeter";

       int x,y,z;
       cin>>x>>y>>z;
       if(x=y){
        if(x=z)cout<<"equletral";
        else cout<<"issoseles";
       }else{     

       }
               
        



}