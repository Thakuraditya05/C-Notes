// coding ninja 
// heaps se acchi tarike se ho jayega 
// heaps me daalo and with [key,value] pairs and waha se pick to bade number such not to have index defferance of 1


// lets try out all subsequance with the given constrau
// pick the subsequance array with no adjucent element 
// f(idx){
//     if(idx==0)return [idx];
//     if(idx<0)return 0;
//     pick=a[idx]+f(idx-2);
//     notPick=0+f(idx-1);
//     return max(pick,notPick);
// }

   






















